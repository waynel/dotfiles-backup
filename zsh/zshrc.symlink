#customization for each shell to lead in itself
  export ZSH=~/dotfiles
  for config_file ($ZSH/zsh/*.zsh) source $config_file
  for config_file ($ZSH/sh/*.sh) source $config_file
  alias rl='source ~/.zshrc'

#specific
  autoload -U compinit
  compinit
  #plugins=(vi-mode)
