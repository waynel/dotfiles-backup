if [ "$#" -lt 1 ]; then
  echo 'Must supply an input file'
  exit 1
else
  if [ "$#" -lt 2 ]; then
    output=output
  else
    output=$2
  fi

  ffmpeg -i $1 -pix_fmt rgb24 -r 10 ${output}.gif
  convert -layers Optimize ${output}.gif ${output}.gif
  cloudapp -d ${output}.gif
fi
