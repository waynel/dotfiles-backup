desc "Remove all extra files before job"
task :remove_extra_latex_files
  sh "rm *.aux" 
  sh "rm *.log"
end

desc "run pdflatex"
task :pdf_latex, :name do |t, args|
  sh "pdflatex #{args.name}.tex"
end

desc "create pdf"
task :maketex

end
